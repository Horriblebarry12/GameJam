using UnityEngine;

public class GameStateScript : MonoBehaviour
{
    //state is intended to determine the state of the game, such as "Not started yet", "Started", "Game Over", and whatever else we need.
    string state = "Start";

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
